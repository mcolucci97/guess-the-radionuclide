# Source recovery

Base: mcolucci97/guess-the-radionuclide, commit b87471e.
The repository contains production bundles, not the original JSX source.
Recovered the complete application-specific JavaScript from assets/index-DrWPOdwe.js, keeping the original database, concepts, parser, tests and UI functions. React and icon runtime code was replaced by package imports; application functions were not rewritten. Local symbol names remain those in the published build.
