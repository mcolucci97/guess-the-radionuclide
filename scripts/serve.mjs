import http from'node:http';import fs from'node:fs/promises';import path from'node:path';const root=path.resolve('dist');http.createServer(async(q,r)=>{try{let name=decodeURIComponent(new URL(q.url,'http://localhost').pathname);if(name.endsWith('/'))name+='index.html';const p=path.resolve(root,'.'+name);if(!p.startsWith(root+path.sep))throw Error();const b=await fs.readFile(p);r.setHeader('Content-Type',({'.html':'text/html','.js':'application/javascript','.css':'text/css','.svg':'image/svg+xml','.wasm':'application/wasm','.json':'application/json','.webmanifest':'application/manifest+json','.png':'image/png'})[path.extname(p)]||'application/octet-stream');r.end(b);}catch{r.writeHead(404);r.end('Not found');}}).listen(4173,'127.0.0.1',()=>console.log('Open http://127.0.0.1:4173/'));
